#include <stdio.h>

#include "s21_math.h"

int main(void) {
  // for (double x = -2 * S21_PI; x <= S21_PI * 2; x += S21_PI / 16)
  //   printf("% .15Lf\n", s21_sin(x) - sin(x));

  // for (double x = -15.; x <= 15.; x += 0.5)
  //   printf("% .15Lf\t\t% .15f\n", s21_exp(x), exp(x));

  return 0;
}